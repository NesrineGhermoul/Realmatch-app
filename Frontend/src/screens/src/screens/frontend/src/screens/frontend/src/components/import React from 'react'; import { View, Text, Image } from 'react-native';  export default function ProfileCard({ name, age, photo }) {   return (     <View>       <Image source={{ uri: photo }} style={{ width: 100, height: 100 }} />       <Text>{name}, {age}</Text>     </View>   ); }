import React from 'react';
import { View, Text, Image } from 'react-native';

export default function ProfileCard({ name, age, photo }) {
  return (
    <View>
      <Image source={{ uri: photo }} style={{ width: 100, height: 100 }} />
      <Text>{name}, {age}</Text>
    </View>
  );
}
